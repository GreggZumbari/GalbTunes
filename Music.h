#ifndef MUSIC_H
#define MUSIC_H

class Music {

  char* title, artist, year, duration, pub;

  Music(char* title, char* artist, char* year, char* duration, char* pub) {
    this->title = title;
    this->year = year;
    this->pub = pub;
    this->rating = rating;
  }
  
}

#endif
